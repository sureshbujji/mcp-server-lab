# Example environment — placeholders only, nothing real.
# This repo runs fully offline; no secrets are required.

# Optional: override the MCP server name shown to clients
MCP_SERVER_NAME=qa-tools

# Optional: log level for the server process (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
